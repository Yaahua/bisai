# bisai 仓库精简后统一执行计划

作者：**Manus AI**  
日期：2026-05-01

## 一、当前结论

本轮重整的核心判断是：此前 v46 与 v46b 的批量候选已经连续出现 `0.4167`、`0.4164`、`0.4162` 三个明显低于真实底座的分数，因此必须把这一路线视为**错误文件体系或错误假设体系**，不得继续沿着“删关系、低频消融、单条微加法”的方式提交。当前仓库应回到唯一可信底座：`submit_v44_del_var_con_cross`，仓库内按你此前记录将其视为约 `0.4514` 的最高可信底座。

> 当前原则：**先保底，再查因；先统一仓库，再谈冲分；不再批量造候选文件。**

## 二、精简后的主工作区

| 类别 | 保留位置 | 用途 |
|---|---|---|
| 可信底座 JSON | `当前工作区/底座/submit_baseline_04514_v44_del_var_con_cross.json` | 后续所有实验只能从该文件复制派生。 |
| 可信底座 ZIP | `当前工作区/底座/submit_baseline_04514_v44_del_var_con_cross.zip` | 必要时用于回传或复核榜单底座。 |
| 校验脚本 | `当前工作区/工具/validate_submission.py` | 任何候选生成后必须先过结构校验。 |
| 打包脚本 | `当前工作区/工具/make_submit_zip.py` | 统一 zip 内部结构，避免上传文件名错误。 |
| 失败禁忌记录 | `当前工作区/记录/失败禁忌_v46_v46b三连0416记录.md` | 明确禁止再次提交同类路线。 |
| 文件指纹 | `当前工作区/文件指纹.md` | 用于核对底座与归档包是否被误改。 |

## 三、已清理内容

v46 与 v46b 的批量候选、交付包、差异表、执行报告和生成脚本已经从主工作区移除。为避免误删后无法回滚，它们被压缩归档到：

`归档/废弃_v46_v46b批量候选_20260501.zip`

主工作区只保留一本失败禁忌记录，而不再暴露任何可误传的 v46/v46b zip 文件。

## 四、下一步只允许做的事情

下一步不再直接造候选提交。唯一允许的动作是对 `0.4514` 底座进行**身份复核**：检查该 zip 内部文件名、JSON 条数、空关系分布、关系总数、样本 ID 顺序，与仓库中声称的最高分底座是否完全一致。如果底座复核通过，再从历史报告中寻找“真实涨分操作”的最小差异；如果底座复核不通过，先修复底座定位，禁止继续冲分。

| 阶段 | 动作 | 产物限制 | 通过标准 |
|---|---|---:|---|
| 1 | 复核 `submit_baseline_04514_v44_del_var_con_cross.zip` 与 JSON 身份 | 只生成 1 个审计摘要 | zip 内部结构、JSON schema、记录数一致。 |
| 2 | 反查历史最高分来源 | 只阅读现有报告，不生成候选 | 找到明确“提交文件名—得分”对应关系。 |
| 3 | 设计下一候选 | 最多 1 个候选 | 必须能解释为何不是 v46/v46b 的 0.416x 错误体系。 |

## 五、硬性禁令

第一，禁止继续提交任何 `v46`、`v46b`、`micro_del`、`micro_add_consensus`、`direction_gene_loi_mrk_delete` 命名的文件。第二，禁止一次生成多个候选。第三，禁止在没有复核底座身份之前修改 JSON 内容。第四，禁止以“学术资料说可能有效”为理由直接上榜提交；所有操作必须回到仓库内真实得分证据。

## 六、仓库工作方式

以后所有操作都以 `当前工作区/` 为入口。旧目录 `数据/A榜/` 仍作为历史资产存在，但不作为直接操作入口。若确需恢复 v46/v46b 垃圾文件，只能从 `归档/废弃_v46_v46b批量候选_20260501.zip` 解压，不得把归档内 zip 直接上传比赛。
