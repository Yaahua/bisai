## 1. 已确认问题

| 序号 | 位置 | 问题类型 | 具体问题 | 直接修复动作 |
| --- | --- | --- | --- | --- |
| 1 | Sample 6 | 关系锚点偏移错误 | 两条 `AFF` 关系都把 head 锚定在前文第一次出现的 `SbMYBHv33[48:57]`，但真正承担“resulted in longer roots and increased salt tolerance”致因语义的是后文再次出现的 `SbMYBHv33[135:144]`。 | 将两条 `AFF` 关系的 head 统一改为 `SbMYBHv33[135:144]`，并同步更新 `head/head_start/head_end/head_type` 字段。 |
| 2 | Sample 10 | 词块边界重叠 | 同一起点同时保留 `QTL[84:87]` 与 `QTLs[84:88]` 两个重叠实体，形成竞争性单复数边界。 | 删除 `QTL[84:87]`，仅保留与原文一致的 `QTLs[84:88]`；若确需保留单数 `QTL`，必须在后文真实出现位置单独重建。 |

## 2. 潜在问题

| 序号 | 位置 | 潜在风险 | 复核结论 |
| --- | --- | --- | --- |
| P1 | Sample 6 | 同一基因多次出现时，关系很容易错误借用前一次提及的位置，造成语义锚点漂移。 | 本样本仅需把两条 `AFF` 关系统一迁移到后文致因分句中的 `SbMYBHv33[135:144]`。 |
| P2 | Sample 10 | 删除短词块后，若仍残留依赖该实体的关系或说明文字，会形成悬空引用或边界不一致。 | 当前问题集中在实体去重；修复后需再次确认该样本不存在引用已删除 `QTL[84:87]` 的关系。 |
| P3 | Sample 10 | 缩写类实体在单复数切换时最易出现“同偏移双实体”，会放大训练噪声。 | 修复后应保证该位置只保留一个与原文完全一致的 `QTLs` 实体，并再次执行 `text[start:end] == entity['text']` 校验。 |

## 3. 关联问题

本块修复时需要把**关系锚点修复**与**实体边界去重**分别做彻底。Sample 6 的核心是把因果关系绑定到真正承担语义功能的那一次基因提及，不能只改 `head` 文本而遗漏其偏移字段。Sample 10 的核心是消除同一偏移上的竞争性实体，避免单数与复数词块共存。修复完成后，应再次核对：其一，所有 `AFF` 关系是否仍与现存实体完全对齐；其二，是否还存在同一位置的重叠缩写实体。
