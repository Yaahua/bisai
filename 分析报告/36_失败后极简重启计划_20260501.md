# 失败后极简重启计划：停止删、只做一个高置信加法

作者：**Manus AI**  
日期：2026-05-01

## 1. 我先纠正前面的判断

你指出得对，前两次结果已经证明我之前把重点放在“删除可疑关系”上是错误方向。第一个 `GENE-LOI-MRK` 删除候选得分 **0.4167**，第二个单条 `GENE-CON-TRT` 微删候选得分 **0.4164**，都远低于当前底座 **0.4514**。这不是普通波动，而是非常强的反证：**公开榜当前更惩罚召回损失，v44 底座中那些看似可疑的关系很可能有不少实际命中；继续删除、消融、低频判噪声会继续掉分。**

外部资料也支持这个纠偏。BioCreative/BioRED 的高分系统更强调 data-centric 数据质量、预处理鲁棒性、多模型/多提示集成和后处理规则，而不是直接删掉低频候选；相关论文使用 PubTator API、多种 PubMedBERT 分类器、实体对提示、上下文和规则集成来提高鲁棒性。[1] 公开榜研究也提醒，反复根据 leaderboard 反馈大批量试错会导致过拟合，下一步必须从“批量候选”改成“一个强假设、一次提交、可解释收益”。[2]

> **结论：立即冻结全部 v46/v46b 删除类候选。下一步只允许从 v44 底座出发，做一个高置信、可解释、单条加法。**

## 2. 重新判断现在的计分信号

两次失败的幅度大约都是 **-0.035**。这反而提供了一个重要信息：公开榜有效命中单元可能很少，单条关系如果是公开榜真阳性，删除它就可能带来约 0.03—0.04 的大幅下降。因此，当前比赛后段不适合靠“删错一点点”提分，而适合寻找 **缺失的一条真阳性关系**。如果单条补充命中，理论上也可能带来同量级上升；如果错了，则可能明显掉分，所以候选必须少而强。

| 已验证动作 | 平台分数 | 对底座影响 | 新解释 |
|---|---:|---:|---|
| v44 底座 `submit_v44_del_var_con_cross` | 0.4514 | 0 | 当前唯一可信底座 |
| 删除 5 条 `GENE-LOI-MRK` | 0.4167 | -0.0347 | 被删关系中至少有公开榜关键真阳性，整类删除错误 |
| 删除 1 条 `GENE-CON-TRT` | 0.4164 | -0.0350 | 单条删除也会严重伤害召回，低频即噪声假设错误 |

## 3. 下一步唯一推荐提交

我建议下一步不要再生成新文件，而是使用已经存在、但尚未上传的单条加法候选：

`数据/A榜/v46b_safe/submit_v46b_micro_add_consensus_01_49_var_con_cross.zip`

这条候选只在样本 49 增加一条关系：

`Aininghuang(VAR) --CON--> F-2 population(CROSS)`

选择它的原因有三点。第一，原文是 “A cross between Aininghuang and Jingu 21 produced an F-2 population...”，语义上 `Aininghuang` 与 `F-2 population` 存在明确构成交叉群体关系。第二，这条关系缺失于 v44 底座，但出现在 6 个历史保守版本中，不是新模型的随机幻觉。第三，它属于 **VAR-CON-CROSS**，训练类型频次为 8，虽然不是高频类型，但比前面那些“低频删除噪声”路线更符合当前应做的 **召回补强**。

| 优先级 | 候选 | 动作 | 为什么只选它 |
|---:|---|---|---|
| 1 | `submit_v46b_micro_add_consensus_01_49_var_con_cross.zip` | 单条加法 | 原文语义强、历史 6 版本共识、只补不删、可归因 |
| 暂不提交 | `submit_v46b_micro_add_consensus_02_49_var_con_cross.zip` | 单条加法 | 与第一条同句、同类型，但应等第一条结果后再决定 |
| 暂不提交 | `submit_v46b_micro_add_consensus_03_131_var_con_cross.zip` | 单条加法 | 语义也合理，但样本 131 底座无 VAR/CROSS 关系，风险略高 |

## 4. 极简执行规则

下一步只上传 **一个文件**：`submit_v46b_micro_add_consensus_01_49_var_con_cross.zip`。如果得分 **高于 0.4514**，说明“历史共识单条加法”方向成立，再考虑第二条同句 `Jingu 21(VAR) --CON--> F-2 population(CROSS)`。如果得分仍低于 0.4514，立即停止所有已生成候选，不再提交 v46/v46b 包，回到 v44 做人工逐条证据表，而不是继续用榜单试错。

| 返回分数 | 判断 | 下一步 |
|---:|---|---|
| > 0.4514 | 单条召回补强成立 | 再提交同句第二条 `Jingu 21 --CON--> F-2 population` |
| = 0.4514 附近 | 公开榜未覆盖或无影响 | 暂停提交，继续人工找更强加法证据 |
| < 0.4514 | 加法方向也不稳 | 停止所有候选，保护 v44，重做人工证据表 |

## 5. 一句话版

**现在不要再删任何关系，不要再批量生成文件。下一步只提交一个现成的、单条、可解释的历史共识加法：`submit_v46b_micro_add_consensus_01_49_var_con_cross.zip`。**

## References

[1]: https://academic.oup.com/database/article/doi/10.1093/database/baae127/8140696 "Enhancing biomedical relation extraction through data-centric and preprocessing-robust ensemble learning approach"

[2]: https://arxiv.org/pdf/1510.03349 "Toward a Better Understanding of Leaderboard"
