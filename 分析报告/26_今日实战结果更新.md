# 今日实战结果更新（2026-04-20）

**作者：Manus AI**

今天的核心变化，是把仓库中的“公开最高分”与用户真实线上最高分彻底对齐，并用真实榜单反馈重新校准了冲分主线。最终结论非常明确：**当前仓库的正确底座是 `submit_v30_fixed`，当前最有效的升级方式是建立在真基线之上的小步纯加法。**

## 一、今日确认的真实基线

此前仓库内部长期把 `v17_whitelist` 的 **0.4224** 当作历史最高分，这一前提在今天被纠正。用户确认，自己真实线上高分版本为 `submit_v30_fixed.zip`，成绩为 **0.4449359367231**，长期位列第二。对该包进行反查后，可以确认它最接近 `submit_v29_postprocessed.json`，并且是在该底座上补入了一批额外关系形成的独立高分版本。

| 项目 | 结果 |
| :--- | :--- |
| 真实高分版本 | `submit_v30_fixed.zip` |
| 真实高分 | **0.4449359367231** |
| 路线判断 | `v29` 风格底座上的增强版 |
| 结论 | 后续所有实验都必须围绕这条真基线展开 |

## 二、今日两次实战反馈

第一轮提交测试的是 `submit_v31_trim_none.zip`。这版采取的是“保守减法”思路，即删除真基线中那些看起来不被现有候选支持的孤立增量。线上结果是 **0.4445**，比真基线更低。这说明真高分包中的某些看似孤立的关系实际上具有真实价值，轻易删除会直接伤害得分。

第二轮提交测试的是 `submit_v32_plus_abs_var.zip`。这版不改动真基线原始结构，只做小步纯加法，仅补入 `ABS-AFF-TRT` 与 `VAR-HAS-TRT` 两类关系共 **56 条**。线上结果达到 **0.4479**，成为新的仓库最高分。

| 提交顺序 | 文件 | 策略 | 分数 | 结论 |
| :--- | :--- | :--- | :---: | :--- |
| 1 | `submit_v31_trim_none.zip` | 真基线减法清理 | **0.4445** | 失败，减法不可取 |
| 2 | `submit_v32_plus_abs_var.zip` | 真基线小步纯加法 | **0.4479** | 成功，成为当前主线 |

## 三、今天最重要的结论

今天真正带来价值的，不只是多提交了两个文件，而是明确了当前路线的“允许做什么、不允许做什么”。

| 方向 | 结论 | 当前建议 |
| :--- | :--- | :--- |
| 对 `submit_v30_fixed` 做删减清理 | 已证伪 | 不再作为优先方向 |
| 在真基线上小步增加高价值关系 | 已验证有效 | 继续作为主线 |
| 大规模扩充多种关系类型 | 暂未验证且风险高 | 谨慎后置 |
| 回退到 `v17` 时代的分析框架 | 已过时 | 不再作为执行基线 |

## 四、当前局面与明日优先级

当前已知第一名分数为 **0.4560**，我方最新分数为 **0.4479**，差距为 **0.0081**。虽然差距仍在，但已经进入一个可以通过更精细微调继续逼近的区间。由于今天提交次数已用完，明日不应大改结构，而应优先测试与 `submit_v32_plus_abs_var` 接近的同类微调版本。

| 明日优先级 | 文件 | 原因 |
| :--- | :--- | :--- |
| 1 | `submit_v32_plus_balanced_70.zip` | 在真基线上继续小步加法，但比激进版本更克制 |
| 2 | `submit_v32_plus_abs_var_crop.zip` | 验证少量 `CROP-CON-VAR` 是否还能安全提升 |
| 3 | `submit_v32_plus_qtl_abs.zip` | 偏激进，放在前两者之后 |
| 4 | `submit_v30_fixed.zip` | 若后续版本连续掉分，作为安全回退点 |

## 五、仓库已完成同步

今天已经把本轮关键结果同步进仓库，包括顶层 `README.md`、`数据/A榜/README.md`、`分析报告/README.md` 与主试错日志 `5_项目交流与试错日志.md`。因此，现在仓库中的公开记录已经与真实线上结果对齐，不再停留在旧的 **0.4224** 阶段。
